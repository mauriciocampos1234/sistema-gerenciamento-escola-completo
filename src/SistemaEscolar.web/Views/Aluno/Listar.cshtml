@using SistemaEscolar.web.Models.Menu
@model IList<SistemaEscolar.web.Models.Aluno.ListarViewModel>
@{
    ViewData["Title"] = "Listar Alunos";
    ViewData["Menu"] = Menu.Aluno;
}

<h1 class="py-4">Lista de Alunos</h1>

<div class="d-flex py-2">
    <a class="btn btn-primary" asp-controller="Aluno" asp-action="Criar">
        Adicionar Aluno
    </a>
</div>

<div class="table-responsive d-flex py-2">
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nome</th>
                <th scope="col">Email</th>
                <th scope="col">Login</th>
                <th scope="col"></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var aluno in Model) // Percorre a lista de alunos
            {
                <tr>
                    <td>@aluno.Id</td>
                    <td>@aluno.Nome</td>
                    <td>@aluno.Email</td>
                    <td>@aluno.Login</td>

                    <td>
                        <a class="link-underline link-underline-opacity-0"
                           asp-controller="Aluno" asp-action="Editar" asp-route-id="@aluno.Id">
                            Editar
                        </a>
                    </td>
                </tr>
            }


        </tbody>

    </table>
</div>
